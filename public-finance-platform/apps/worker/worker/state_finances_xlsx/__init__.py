"""Loader for the RBI 'State Finances' xlsx tables.

These are the source-of-truth tables RBI publishes alongside the PDF report;
they're cleaner to parse than the PDFs and carry every state, so they enable
both AP-first views and AP-vs-peers comparison from the same fact rows.
"""
