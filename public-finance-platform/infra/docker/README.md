# Docker Infrastructure

This folder is reserved for additional Docker assets (compose overrides, custom images, and production deployment manifests).

Current local development setup uses [../../docker-compose.yml](../../docker-compose.yml).
